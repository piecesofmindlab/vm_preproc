Pseudo-code to run preprocesing step: 

class PreprocStep(object):
    def __init__(self, 
                 fn_name, 
                 params, 
                 db_save=False,
                 concatenate_parts=False,

                 )



if self.db_save:
    vmt.file_io.save_arrays(self.fpath, s=s)

# Specify axis here
if self.concatenate_parts is not False:
    np.

